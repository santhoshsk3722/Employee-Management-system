apiVersion: v1
kind: Service
metadata:
  name: employee-management
  namespace: employee-management
spec:
  selector:
    app: employee-management

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080

  type: NodePort
